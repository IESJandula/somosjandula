# Entorno de despliegue: los hosts de los microservicios se derivan como
# https://pro<servicio>.iesjandula.es (ver src/environment/apiUrls.ts).
# Dokploy puede sobreescribir VITE_ENVIRONMENT (pre/pro) como Build Arg.
VITE_ENVIRONMENT=pro
VITE_API_DOMAIN=iesjandula.es
